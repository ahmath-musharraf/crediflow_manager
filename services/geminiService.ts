// This service is currently unused as AI features have been disabled.
// It is kept for potential future re-integration.

import { Transaction } from "../types";

export const analyzeSalesTrends = async (transactions: Transaction[]) => {
  return "AI analysis disabled.";
};